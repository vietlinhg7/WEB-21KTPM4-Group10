<body>
  <main>
    <div class="me-md-2 m-3 mx-5 px-3">
<button class="btn btn-secondary dropdown-toggle bg-primary" type="button" id="dropdownMenuButton1"
            data-bs-toggle="dropdown" aria-expanded="false">
            Tạo mới
          </button>
          <!-- Dropdown menu -->
          <div class="dropdown-menu " aria-labelledby="dropdownMenuButton1">
            <!-- Dropdown items -->
            <form onsubmit="addItem(event)">
                <div class="mb-3">
                    <label for="" class="form-label">Tên Quận</label>
                <input type="text" class="form-control" name="keyword" id="quanID" aria-describedby="helpId" placeholder="" required>
                </div>
            </form>
          </div>

    </div>
   
  <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.6/dist/umd/popper.min.js"
    integrity="sha384-oBqDVmMz9ATKxIep9tiCxS/Z9fNfEXiDAYTujMAeBAsjFuCZSmKbSSUnQlmh/jp3" crossorigin="anonymous">
    </script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.1/dist/js/bootstrap.min.js"
    integrity="sha384-7VPbUDkoPSGFnVtYi0QogXtr74QeVeeIs99Qfg5YCF+TidwNdjvaKZX19NZ/e6oz" crossorigin="anonymous">
    </script>
  <script>
    function addItem(e) {
      const Quan = require('../models/quan');
      const Phuong = require('../models/phuong');
      const newQuan = new Quan({
          quanID: document.getElementById('quanID').value,
        });

        try {
          const savedQuan = await newQuan.save();
          // handle success
        } catch (error) {
          console.error(`Error occurred while adding user: ${error}`);
          // handle error
        }
      // clear the input value after submitting
      document.getElementById('quanID').value = '';
    }

  item.value = '';
  </script>
</body>